import { GoogleMapLinkPipe } from './google-map-link.pipe';

describe('GoogleMapLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new GoogleMapLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
